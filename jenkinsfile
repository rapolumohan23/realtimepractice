pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                sh 'docker build -t demo-app:latest .'
                sh 'docker run demo-app:latest'
            }
        }
    }
}
