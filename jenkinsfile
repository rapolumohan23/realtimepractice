pipeline {
    agent {
       
    }

    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t hello-node-app:latest .'
            }
        }
    }
}

