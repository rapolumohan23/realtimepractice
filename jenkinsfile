pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                sh 'docker build -t demo-app:latest .'
                sh 'docker run -p 7000:7000 demo-app:latest'
            }
        }
    }
}
