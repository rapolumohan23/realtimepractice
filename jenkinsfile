pipeline {
    agent any

    stages {
        stage('Docker Build') {
            steps {
                sh 'docker build -t demo-app:latest .'
                sh 'docker run -p 6000:6000 demo-app:latest'
            }
        }
    }
}
